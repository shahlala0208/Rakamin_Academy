const Movie = require('../models/Movie');

// Fungsi untuk mengunggah foto
const uploadPhoto = (req, res) => {
  // Logic untuk mengunggah foto
};

// Fungsi CRUD lainnya (create, read, update, delete)
// ...

module.exports = {
  uploadPhoto,
  // Export fungsi CRUD lainnya
};
